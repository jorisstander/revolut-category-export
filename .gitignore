node_modules/
*.har
*.csv
exports/
.DS_Store
*.log
.env
# Design sources live here while the popup is being worked on. They bundle
# third-party editor JavaScript that loads from remote CDNs, which would
# contradict this project's zero-dependency claim if it were ever committed.
docs/design/
dist/

# Drafts for the trademark and terms-of-service question: correspondence and
# instructions to a lawyer. This repository is public, and none of that should
# become so by an absent-minded `git add`. Committing one deliberately still
# works with `git add -f`.
docs/legal/

# Chrome Web Store scaffolding. The submission copy and the listing image are
# operational, not part of the extension, and restating the permission
# justifications next to PRIVACY.md only gives them somewhere to drift. Kept on
# disk as the record of what was submitted.
docs/store-listing.md
docs/store-assets/
